/**
 * Visual constants aligned with the CASIFLY web brand (deep indigo) so shell,
 * splash, WebView backing color, and loading states remain consistent — this
 * minimizes perceived “white flashes” before the SPA paints.
 */
export const brandColors = {
  screenBg: '#312e81',
  screenBgAlt: '#0f172a',
  surfaceText: '#e0e7ff',
  mutedText: '#c7d2fe',
  accent: '#818cf8',
  errorBg: '#1e1b4b',
} as const;
